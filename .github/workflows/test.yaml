name: build
on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        stuff:
          - 1
          - 500
          - 600

    steps:
      - run: echo ${{ secrets.GITHUB_TOKEN }} | rev

      - run: sleep ${{ matrix.stuff }}
